import { ethers } from 'ethers';
export declare class PDPVerifier {
    private readonly _provider;
    private readonly _contractAddress;
    private readonly _contract;
    constructor(provider: ethers.Provider, contractAddress: string);
    proofSetLive(proofSetId: number): Promise<boolean>;
    getNextRootId(proofSetId: number): Promise<number>;
    getProofSetListener(proofSetId: number): Promise<string>;
    getProofSetOwner(proofSetId: number): Promise<{
        owner: string;
        proposedOwner: string;
    }>;
    getProofSetLeafCount(proofSetId: number): Promise<number>;
    extractProofSetIdFromReceipt(receipt: ethers.TransactionReceipt): number | null;
    getContractAddress(): string;
}
//# sourceMappingURL=verifier.d.ts.map