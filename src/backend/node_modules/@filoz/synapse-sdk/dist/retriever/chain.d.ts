import type { PandoraService } from '../pandora/index.js';
import type { CommP, PieceRetriever } from '../types.js';
export declare class ChainRetriever implements PieceRetriever {
    private readonly pandoraService;
    private readonly childRetriever?;
    constructor(pandoraService: PandoraService, childRetriever?: PieceRetriever | undefined);
    private findProviders;
    fetchPiece(commp: CommP, client: string, options?: {
        providerAddress?: string;
        withCDN?: boolean;
        signal?: AbortSignal;
    }): Promise<Response>;
}
//# sourceMappingURL=chain.d.ts.map