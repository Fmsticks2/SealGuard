import type { CommP, PieceRetriever, ApprovedProviderInfo, SubgraphRetrievalService } from '../types.js';
export declare class SubgraphRetriever implements PieceRetriever {
    private readonly subgraphService;
    private readonly childRetriever?;
    constructor(subgraphService: SubgraphRetrievalService, childRetriever?: PieceRetriever | undefined);
    findProviders(commp: CommP, providerAddress?: string): Promise<ApprovedProviderInfo[]>;
    fetchPiece(commp: CommP, client: string, options?: {
        providerAddress?: string;
        signal?: AbortSignal;
    }): Promise<Response>;
}
//# sourceMappingURL=subgraph.d.ts.map