import type { CommP, FilecoinNetworkType, PieceRetriever } from '../types.js';
export declare class FilCdnRetriever implements PieceRetriever {
    private readonly baseRetriever;
    private readonly network;
    constructor(baseRetriever: PieceRetriever, network: FilecoinNetworkType);
    hostname(): string;
    fetchPiece(commp: CommP, client: string, options?: {
        providerAddress?: string;
        withCDN?: boolean;
        signal?: AbortSignal;
    }): Promise<Response>;
}
//# sourceMappingURL=filcdn.d.ts.map